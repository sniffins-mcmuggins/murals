services:

  # ── API ────────────────────────────────────────────────────────────────────
  api:
    build:
      context: ../api
      target: dev
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      DATABASE_URL: "postgres://render:render@db:5432/render?sslmode=disable"
      MINIO_ENDPOINT: "minio:9000"
      MINIO_PUBLIC_ENDPOINT: "localhost:9000"
      MINIO_ACCESS_KEY: "${MINIO_ACCESS_KEY:-renderdev}"
      MINIO_SECRET_KEY: "${MINIO_SECRET_KEY:-renderdev123}"
      MINIO_BUCKET: "${MINIO_BUCKET:-render-images}"
      MINIO_USE_SSL: "false"
      CDN_BASE_URL: "http://localhost:9000/render-images"
      JWT_SECRET: "${JWT_SECRET:-dev-jwt-secret-change-in-prod}"
      LOG_LEVEL: "${LOG_LEVEL:-debug}"
      # Every Playwright worker shares the same source IP from the API's
      # POV, so the prod default of 5/min trips after ~5 logins. Loosen
      # here for dev/test — prod keeps the config.go default (5/min).
      LOGIN_RATE_LIMIT_PER_MIN: "${LOGIN_RATE_LIMIT_PER_MIN:-120}"
      LOGIN_RATE_LIMIT_BURST: "${LOGIN_RATE_LIMIT_BURST:-60}"
      SITE_BASE_URL: "${SITE_BASE_URL:-http://localhost:3000}"
      STRIPE_SECRET_KEY: "${STRIPE_SECRET_KEY:-}"
      STRIPE_WEBHOOK_SECRET: "${STRIPE_WEBHOOK_SECRET:-}"
      STRIPE_ARTIST_BASIC_ANNUAL_PRICE_ID: "${STRIPE_ARTIST_BASIC_ANNUAL_PRICE_ID:-}"
      STRIPE_ARTIST_BASIC_MONTH_PRICE_ID: "${STRIPE_ARTIST_BASIC_MONTH_PRICE_ID:-}"
      STRIPE_ARTIST_PRO_ANNUAL_PRICE_ID: "${STRIPE_ARTIST_PRO_ANNUAL_PRICE_ID:-}"
      STRIPE_ARTIST_PRO_MONTH_PRICE_ID: "${STRIPE_ARTIST_PRO_MONTH_PRICE_ID:-}"
      STRIPE_ORG_SETUP_PRICE_ID: "${STRIPE_ORG_SETUP_PRICE_ID:-}"
      STRIPE_FESTIVAL_ACTIVATION_PRICE_ID: "${STRIPE_FESTIVAL_ACTIVATION_PRICE_ID:-}"
      STRIPE_FESTIVAL_ANNUAL_PRICE_ID: "${STRIPE_FESTIVAL_ANNUAL_PRICE_ID:-}"
    volumes:
      - ../api:/app
      - api_tmp:/app/tmp
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
    restart: unless-stopped

  # ── WEB ────────────────────────────────────────────────────────────────────
  web:
    build:
      context: ..
      dockerfile: web/Dockerfile
      target: dev
    working_dir: /workspace/web
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: "http://localhost:8080"
      API_URL: "http://api:8080"
      NODE_ENV: "development"
      NEXT_PUBLIC_STRIPE_ARTIST_BASIC_ANNUAL: "${NEXT_PUBLIC_STRIPE_ARTIST_BASIC_ANNUAL:-}"
      NEXT_PUBLIC_STRIPE_ARTIST_BASIC_MONTHLY: "${NEXT_PUBLIC_STRIPE_ARTIST_BASIC_MONTHLY:-}"
      NEXT_PUBLIC_STRIPE_ARTIST_PRO_ANNUAL: "${NEXT_PUBLIC_STRIPE_ARTIST_PRO_ANNUAL:-}"
      NEXT_PUBLIC_STRIPE_ARTIST_PRO_MONTHLY: "${NEXT_PUBLIC_STRIPE_ARTIST_PRO_MONTHLY:-}"
    volumes:
      - ../:/workspace
      - web_node_modules:/workspace/web/node_modules
      - web_next:/workspace/web/.next
    depends_on:
      - api
    restart: unless-stopped

  # ── DATABASE ───────────────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: render
      POSTGRES_PASSWORD: render
      POSTGRES_DB: render
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U render -d render"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  # ── MINIO ──────────────────────────────────────────────────────────────────
  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"   # S3 API
      - "9001:9001"   # Console
    environment:
      MINIO_ROOT_USER: "${MINIO_ACCESS_KEY:-renderdev}"
      MINIO_ROOT_PASSWORD: "${MINIO_SECRET_KEY:-renderdev123}"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  # ── MINIO INIT ─────────────────────────────────────────────────────────────
  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ACCESS_KEY: "${MINIO_ACCESS_KEY:-renderdev}"
      MINIO_SECRET_KEY: "${MINIO_SECRET_KEY:-renderdev123}"
      MINIO_BUCKET: "${MINIO_BUCKET:-render-images}"
    entrypoint: >
      /bin/sh -c "
        mc alias set local http://minio:9000 $$MINIO_ACCESS_KEY $$MINIO_SECRET_KEY &&
        mc mb --ignore-existing local/$$MINIO_BUCKET &&
        mc anonymous set download local/$$MINIO_BUCKET &&
        echo 'MinIO bucket ready'
      "
    restart: "no"

  # ── PROMETHEUS ─────────────────────────────────────────────────────────────
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.console.libraries=/etc/prometheus/console_libraries"
      - "--web.console.templates=/etc/prometheus/consoles"
    restart: unless-stopped

volumes:
  db_data:
  minio_data:
  prometheus_data:
  api_tmp:
  web_node_modules:
  web_next:
